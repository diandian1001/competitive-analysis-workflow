# Hermes 适配

## 可用工具时

- 使用搜索 MCP 或 API 进行候选池扫描和证据采集。
- 使用 `delegate_task` 时按“竞品对象”或“证据类型”拆分，避免多个子任务重复搜索。
- 使用 terminal / Python 处理表格、评分和图表。
- 汇总前去重来源、统一时间范围和评价口径。

## 工具不可用时

回退到核心 `SKILL.md`。明确说明未完成的实时搜索、API 数据或产品体验，不得用推测补齐。

## 子任务建议

1. 市场候选池
2. 官方产品证据
3. 用户反馈证据
4. 我方资料基线
5. 反证与风险审查
